---
title: "54 supported languages for lemmatization"
description: "The 54 languages Simplemma lemmatizes, with dictionary size and measured accuracy per language, from Arabic and German to Tagalog and Ukrainian."
---

# Supported languages

Simplemma lemmatizes 54 languages: Arabic (`ar`), Asturian (`ast`), Bulgarian (`bg`), Catalan (`ca`), Czech (`cs`), Welsh (`cy`), Danish (`da`), German (`de`), Greek (`el`), English (`en`), Middle English (`enm`), Esperanto (`eo`), Spanish (`es`), Estonian (`et`), Persian (`fa`), Finnish (`fi`), French (`fr`), Irish (`ga`), Gaelic (`gd`), Galician (`gl`), Ancient Greek (`grc`), Manx (`gv`), Serbo-Croatian (`hbs`), Hebrew (`he`), Hindi (`hi`), Hungarian (`hu`), Armenian (`hy`), Indonesian (`id`), Icelandic (`is`), Italian (`it`), Georgian (`ka`), Latin (`la`), Luxembourgish (`lb`), Lithuanian (`lt`), Latvian (`lv`), Macedonian (`mk`), Malayalam (`ml`), Malay (`ms`), Norwegian (Bokmål) (`nb`), Dutch (`nl`), Norwegian (Nynorsk) (`nn`), Polish (`pl`), Portuguese (`pt`), Romanian (`ro`), Russian (`ru`), Northern Sámi (`se`), Slovak (`sk`), Slovene (`sl`), Albanian (`sq`), Swedish (`sv`), Swahili (`sw`), Tagalog (`tl`), Turkish (`tr`), Ukrainian (`uk`).

The table below gives the dictionary size and the measured accuracy for each of them.

{%
   include-markdown "../README.md"
   start="<!-- include:languages:start -->"
   end="<!-- include:languages:end -->"
%}
